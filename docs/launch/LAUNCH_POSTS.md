# Launch Posts (copy-ready)

## Main X post

Most AI demos stop at chat.

I built ZRT IoT Maker — a recursive agentic commissioning lab for cyber-physical AI.

Telemetry → agents → tool call → eval gate → Operator Gate → IoT dispatch simulation → proof hash → verified memory.

5-second demo.

Zero secrets.
No direct PLC control.

Live demo: https://tsubaki-nakashima-ai-zrt.vercel.app/

## Technical X thread

1/ I built a bounded, operator-gated recursion loop for IoT commanding in prototype mode.

2/ It starts with telemetry anomaly detection and runs multi-agent phases: diagnose, retrieve evidence, propose tools, and evaluate risk.

3/ An explicit Operator Gate blocks direct command execution.

4/ IoT dispatch remains simulation-first in demo mode. This is not a production control surface.

5/ Proof hashes are emitted to XRPL/Hedera contexts; raw telemetry or prompts are not written on-chain.

6/ I paired the prototype with a 300+ source library and claim matrix to keep the architecture evidence-based.

7/ Repo: not public in this handoff package.

## LinkedIn post

I am sharing a public-ready portfolio handoff for ZRT IoT Maker: an independent recursive agentic commissioning prototype for cyber-physical AI.

It demonstrates: telemetry-driven anomaly detection, multi-agent diagnosis, structured tool proposal, eval gating, operator approval, simulated IoT dispatch, evidence packet generation, and hash-only proof anchoring.

This package includes:

- 22-section README and safety model
- 300+ source library with claim matrix
- launch report, launch posts, demo script, and screenshot checklist
- source-grounded public proof trail

Built for portfolio review under explicit demo / operator-gated boundaries.

## OpenAI/tool-calling angle

If you track tool-calling production transitions:

- Structured outputs and provider tool contracts are treated as implementation contracts, not UX add-ons.
- Operator-gated dispatch is the safety-critical boundary.
- Evidence packets and eval logging are first-class.

ZRT IoT Maker is a prototype that demonstrates how to keep tool-calling loops bounded.

## Gemini/tool-calling angle

Gemini tooling is integrated as a mock/test provider route in a bounded workflow:

- Structured suggestion output
- eval checks before action
- operator approval before dispatch
- no direct cloud command writes

The same architecture applies across provider adapters in a source-grounded way.

## Claude/MCP angle

Claude and MCP-style workflows are treated as composable tool channels with explicit boundaries:

- explicit tool registry
- structured proposals
- guardrails and eval gate
- operator approval before simulation command progression

This is a safer pattern than autonomous tool execution.

## Meta/Llama/open models angle

Open-model integration is modeled through provider-agnostic provider slots with explicit approval and no autonomous actuation:

- model choice is configurable in the provider arena
- proposals remain structured
- safety is enforced by the operator gate
- proof/telemetry evidence stays separated from control path

## Supabase/Vercel/AWS angle

State, logs, and frontend surfaces are layered with app-router boundaries:

- Next.js route model for operator workflows
- Supabase for state/log persistence
- Vercel-oriented build/deploy posture
- AWS IoT bridge hooks in mock/connected readiness mode only

## XRPL/Hedera proof-ledger angle

Proof anchoring is implemented as hash-first evidence:

- XRPL/Hedera compatibility paths are set for testnet contexts
- no secrets or raw prompts/telemetry on-chain
- operator-controlled workflow remains required

The focus is traceability, not machine control.

## Safety/Operator Gate angle

Operator Gate is the primary safety boundary:

- AI proposes only
- operator approves before dispatch
- no direct PLC write tool
- logs retained for review and replay

Independent prototype framing: no official endorsement implied.

## Optional short variant (for launch copy)

5-second reflex, not 5-second hype.

From anomaly to proof with explicit operator governance.

ZRT IoT Maker is a bounded demo for operator-safe cyber-physical AI loops.
